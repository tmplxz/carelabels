from setuptools import setup, find_packages

setup(
    name='certification-suite',
    version='0.0.1',
    description='Certification suite',
    author='Anonymous Author',
    author_email='/dev/null',
    packages=find_packages(),  # same as name
)
